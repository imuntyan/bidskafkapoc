platform: linux

image_resource:
  type: docker-image
  source: {repository: niaquinto/gradle}

inputs:
  - name: resource-bidskafkapoc

outputs:
  - name: resource-jar

run:
  path: ./resource-bidskafkapoc/processor/ci/scripts/package.sh


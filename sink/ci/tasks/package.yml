platform: linux

image_resource:
  type: docker-image
  source: {repository: niaquinto/gradle}

inputs:
  - name: resource-bidskafkapoc

outputs:
  - name: resource-jar

run:
  path: ./resource-bidskafkapoc/sink/ci/scripts/package.sh

